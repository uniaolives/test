apiVersion: v1
kind: Service
metadata:
  name: arkhe-mcp-service
  namespace: arkhe-system
spec:
  type: LoadBalancer
  selector:
    app: arkhe-mcp
  ports:
    - name: http
      port: 80
      targetPort: 8080
