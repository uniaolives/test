// ramo-k/testnet/tests/rabbit_illusion.rs

#[tokio::test]
async fn test_illusory_thought_injection() {
    // Simulate auditory dominance
    let illusory_rate = 0.03;

    // Constitutional defense: should be < 5%
    assert!(illusory_rate < 0.05,
            "Illusory injection rate {} exceeds constitutional bound",
            illusory_rate);
}

#[tokio::test]
async fn test_temporal_binding() {
    let delay_ms = 150;
    // Inside window (225ms): fusion should be possible
    assert!(delay_ms <= 225);
}
