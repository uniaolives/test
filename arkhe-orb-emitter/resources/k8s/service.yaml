apiVersion: v1
kind: Service
metadata:
  name: arkhe-orb-emitter
  namespace: arkhe-system
spec:
  selector:
    app: orb-emitter
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
