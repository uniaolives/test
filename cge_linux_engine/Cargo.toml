[package]
name = "cge-linux-engine"
version = "1.0.0"
edition = "2021"

[dependencies]
nix = { version = "0.27", features = ["process", "ptrace", "sched"] }
libc = "0.2"
blake3 = "1.5"
serde = { version = "1.0", features = ["derive"] }
tracing = "0.1"
thiserror = "1.0"
